package com.mdc.mdc;

import android.util.Log;

/**
 * Created by pavant on 10/14/15.
 */
public class ResetPwd {

    public ResetPwd(){

    }

    /*
    public boolean checkPwdExisting(){
        String storedPwd = saveData.fetchData("Password").toString();
        Log.d("checkPwdExisting length", "Password : " + storedPwd + " : " + storedPwd.length());
        boolean check = true;
        if(storedPwd.length() == 0)
            check = false;

        Log.d("checkPwdExisting:::","" + check);
        return check;

    }*/
}
